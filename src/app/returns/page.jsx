import React from 'react';
import ReturnsPage from '../../views/ReturnsPage.jsx';

export const metadata = {
  title: 'Returns & Exchanges — Stop & Shop',
  description: 'Learn about our 7-day hassle-free returns and exchange policy.',
};

export default function Page() {
  return <ReturnsPage />;
}
