import React from 'react';
import CheckoutPage from '../../views/CheckoutPage.jsx';

export const metadata = {
  title: 'Secure Checkout — Stop & Shop',
  description: 'Complete your order with SSL encrypted secure transaction payment methods.',
};

export default function Page() {
  return <CheckoutPage />;
}
