/**
 * @fileoverview ClothingHeroScene — now retired in favour of the editorial hero image approach.
 * Kept as a no-op export so any lingering imports don't break the build.
 */
const ClothingHeroScene = () => null;
export default ClothingHeroScene;
